#include "help_general"

void main()
{
    object oPC = GetFirstPC();

    SceneSpeak(oPC, "[The doors to the lower levels have been sealed for the night.  You cannot pass this way at the moment...]");
}
