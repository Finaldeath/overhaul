#include "nw_i0_henchman"

int StartingConditional()
{
    return FALSE;

    // No longer check for level recommendations
    /*
    int iResult;

    if(GetHenchman(GetPCSpeaker()) != OBJECT_INVALID || GetCanWork(GetPCSpeaker(), OBJECT_SELF) == FALSE)
    {
     iResult = TRUE;
    }
    else
    {
     iResult = FALSE;
    }
    return iResult;
    */
}
