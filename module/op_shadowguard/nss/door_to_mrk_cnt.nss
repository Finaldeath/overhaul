#include "help_general"

void main()
{
    object oPC = GetFirstPC();

    SceneSpeak(oPC, "[This gate has been magically sealed by the power of the Control Stone.  You cannot pass through while the shield is still in place...]");
}
