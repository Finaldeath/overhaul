void main()
{
    string s;

    s = "IT_SU_HENCH_" + "MARK";
    CreateItemOnObject(s, GetPCSpeaker());

    s = "IT_SU_HENCH_" + "LYEN";
    CreateItemOnObject(s, GetPCSpeaker());

    s = "IT_SU_HENCH_" + "KARA";
    CreateItemOnObject(s, GetPCSpeaker());

    s = "IT_SU_HENCH_" + "BRUK";
    CreateItemOnObject(s, GetPCSpeaker());

    s = "IT_SU_HENCH_" + "TARI";
    CreateItemOnObject(s, GetPCSpeaker());
}
