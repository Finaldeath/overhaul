// sets the "Durnan Spoke" global to 2
// indicating that Durnan has already given his second speech
// also sets the custom strings for the PC hero

void main()
{
    SetLocalInt(GetModule(), "DurnanSpoke", 2);



}
