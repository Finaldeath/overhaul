// this script marks that Durnan has spoken to the hero
// in the well room after the second attack
// it should transfer all the PC's upstairs by Tamsil
// and tell her to initiate dialogue

void main()
{
    SetLocalInt(GetModule(), "Tamsil_Speak", 1);
    // rest of the script to be added
}
